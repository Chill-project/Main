{% extends 'ChillMainBundle::layout.html.twig' %}

{% block content -%}
    <h1>{{ 'Scope creation'|trans }}</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_scope') }}">
            {{ 'Back to the list'|trans }}
        </a>
    </li>
</ul>
{% endblock %}
